language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-reinos-bootstrap' ]; then cd .. && eval "mv $currentfolder generator-reinos-bootstrap" && cd generator-reinos-bootstrap; fi

